<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fitnessapp">

    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ShopActivity"></activity>
        <activity android:name=".ChatLobby" />
        <activity android:name=".SearchFoodDatabase" />
        <activity android:name=".HomeScreen" />
        <activity android:name=".ChangeEntryData" />
        <activity android:name=".WorkoutLog" />
        <activity android:name=".AddTrackerData" />
        <activity android:name=".CalorieTracker" />
        <activity android:name=".FriendsListActivity" />
        <activity android:name=".RegisterAccount" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>